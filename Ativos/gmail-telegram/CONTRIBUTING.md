# Contributing

- preserve the Telegram command surface unless a change is intentional
- run `python -m py_compile *.py` before committing
- keep `README.md`, `config.example.py` and `.env.example` in sync
- do not commit `.env`, credentials or generated database files
